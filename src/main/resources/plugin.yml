name: IOSpawn
version: '${project.version}'
main: eu.mixeration.iospawn.iospawn.IOSpawn
api-version: 1.18
prefix: IOSpawn
load: STARTUP
authors: [Mixeration]

commands:
    IOSpawn:
        usage: /IOSpawn
        aliases: [Spawn]
